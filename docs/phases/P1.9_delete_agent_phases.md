# L3 — P1.9 Delete obsolete agent phases

| Field | Value |
|---|---|
| **id** | `P1.9` |
| **title** | Delete obsolete agent phases |
| **status** | Planning drafted — **no code until L4 gate** |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P1.9 |
| **depends_on** | `P1.0` (inventory); prefer after `P1.6` so docs/agents align with code direction |

## architecture_refs

- [07_PLANNING_STANDARDS.md](../07_PLANNING_STANDARDS.md) §7 — agentic roles; old YAML is not current truth  
- [08](../08_PURGE_AND_SCHEMA_PHASES.md) OD-08-2 — **delete**, no archive  
- [P1.0 Appendix E, B.5](./P1.0_purge_inventory.md)  
- [AGENTS.md](../../AGENTS.md) — points at docs 01–08 + phases  

## goal

Delete obsolete `.agent` phase YAML and pipelines so future Implementer agents cannot load the old platform/desk programme as instructions.

## in_scope

- Delete **all** files listed in [P1.0 Appendix E](./P1.0_purge_inventory.md) under `.agent/phases/`.  
- Delete pipelines:

| Path |
|---|
| `.agent/pipelines/coral-phase.json` |
| `.agent/pipelines/contact-agent-cc.json` |
| `.agent/pipelines/product-validation.json` |
| `.agent/pipelines/validation-v1.json` |

- If `.agent/phases/` or `.agent/pipelines/` empty, leave empty dirs **or** remove dirs; do not leave placeholder YAML.  
- Update any repo doc that lists those phase ids as active (prefer `AGENTS.md` / `docs/phases` already correct).  
- Skim `tools/agent-runner/*.ps1` for hard-coded pipeline paths; if they fail without files, add a one-line comment in runner README or script header: “pipelines removed pending new L3 catalog” — **do not** invent new pipeline JSON in this phase.

## out_scope

- Deleting `.agent/config.yaml`, `secrets.example.json`, `templates/`  
- Touching `.agent/secrets.local.json` (gitignored; never commit)  
- Authoring new phase YAML for graph runtime  
- Changing `tests/agent/**` harness (P1.11)  

## forbidden

- Moving files to `archive/agent-phases-old/`.  
- Leaving a `README` inside phases that says “run phase-a for desk.”  
- Re-adding caption/copilot/mom job phases.  

## exit_criteria

- Appendix E paths absent.  
- Four pipeline JSON files absent.  
- `AGENTS.md` does not reference deleted phase ids as current work.  

## edge_cases

1. Local agent-runner still pointing at deleted JSON — expect fail-fast; acceptable.  
2. Historical commits still contain YAML — OK; working tree must not.  

## verification

```text
# .agent/phases must have zero *.yaml
# .agent/pipelines must have zero obsolete json listed above
```

## rollback

Restore from git history (only if explicitly required).
