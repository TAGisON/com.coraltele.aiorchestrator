# L3 — P2.2 Session

| Field | Value |
|---|---|
| **id** | `P2.2` |
| **title** | Session field lock |
| **status** | Planning drafted — no DDL until L4 |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P2.2 |
| **depends_on** | `P2.0`; coordinates with P2.7 for flow pin |

## architecture_refs

- [06_APPLICATION_FLOW.md](../06_APPLICATION_FLOW.md) — lifecycle  
- [04_LIVE_TURN_MACHINE.md](../04_LIVE_TURN_MACHINE.md) — Ending  
- [P2.0](./P2.0_schema_principles.md) P2.0-P3, P12  
- Existing: `session` in `001_phase_b.sql` + later ALTERs  

## goal

Lock canonical live CC session columns and deprecate unused ones explicitly.

## in_scope — current columns → decision

| Column (today) | Decision | Notes |
|---|---|---|
| `id` | Keep | Session id PK |
| `tenant_id` | Keep | |
| `profile_id` | **Transition** | Keep until P2.7 flow pin exists; then add `flow_id` + `flow_version`; deprecate profile pin when flow runtime ships (named end in P2.7/P2.12 transition note) |
| `profile_version` | **Transition** | Same |
| `clock` | Keep or reshape | Wall-clock / skew helper string — keep until runtime phase proves unused |
| `state` | Keep | Closed vocabulary locked below |
| `owner_instance` | Keep | Orch instance lease |
| `canonical_sample_rate_hz` | Keep | Media |
| `coral_user_id` | Keep nullable | Coral identity if present |
| `caller` JSONB | Keep | ANI / caller_id_name etc. |
| `recording_ref` | Keep | Detail lifecycle in P2.5 (may add sibling columns) |
| `metadata` JSONB | Keep | Non-secret bag; forbid PCM |
| `gateway_binding` JSONB | Keep | From 004 |
| `created_at` / `updated_at` | Keep | |

### Add (when flow model exists — schedule under P2.7 migrate, listed here for session shape)

| Column | Type | Notes |
|---|---|---|
| `flow_id` | TEXT NULL | Published flow registry id |
| `flow_version` | INT NULL | Immutable version pin |
| `active_language` | TEXT NULL | Current Talk language |
| `turn_state` | TEXT NULL | Optional mirror of live turn machine state for ops ([04](../04_LIVE_TURN_MACHINE.md)); if too chatty, audit-only — decide at runtime phase; schema allows NULL |

### Session `state` closed vocabulary (V1 draft)

| Value | Meaning |
|---|---|
| `created` | Row exists; not live |
| `live` | Media/talk active |
| `ending` | Teardown in progress |
| `completed` | Clean end |
| `failed` | System failure end |

(Exact strings may match current code enums at L4 — **update this table to match code inventory in P1.0 follow-up grep before L4**; do not invent drift.)

## out_scope

- Creating `flow_*` tables (P2.7)  
- Recording stop implementation  
- Disposition columns (P2.6 may use `session_disposition` table instead)  

## forbidden

- Dual-writing desk id onto session as dialogue source after P2.7.  
- Storing full transcript inside `metadata`.  

## exit_criteria

- Field table Locked.  
- Deprecated columns listed (none force-dropped in this phase).  
- Note: additive `flow_*` pins ship with P2.7 migration, not a premature P2.2-only migrate.  

## edge_cases

1. Session without flow pin during post-P1 dialogue-offline era — allowed (`flow_*` NULL).  
2. Recording_ref set but stop never recorded — P2.5.

## verification

Diff this table against `001` + ALTERs + Go `Session` struct before L4.

## rollback

Planning only.
