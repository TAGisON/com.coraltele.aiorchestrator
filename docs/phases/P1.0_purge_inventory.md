# L3 — P1.0 Purge inventory lock

| Field | Value |
|---|---|
| **id** | `P1.0` |
| **title** | Purge inventory lock |
| **status** | Planning Locked (inventory); **owner signed 2026-09-04** |
| **parent_plan** | [08_PURGE_AND_SCHEMA_PHASES.md](../08_PURGE_AND_SCHEMA_PHASES.md) § P1.0, Part A |
| **depends_on** | Doc 07 process; OD-08-1…5 SETTLED in doc 08 |
| **inventory_date** | 2026-09-04 (branch `docs/llm-callcentre-architecture`) |

## architecture_refs

- [02_CURRENT_STATE.md](../02_CURRENT_STATE.md) — keep media pipe; discard desk hybrid + old consoles  
- [01_VISION_AND_SCOPE.md](../01_VISION_AND_SCOPE.md) — V1 voice CC only  
- [03_BRAIN_AND_GRAPH.md](../03_BRAIN_AND_GRAPH.md) — new brain replaces `internal/desk`  
- [05_MEDIA_AND_VENDORS.md](../05_MEDIA_AND_VENDORS.md) — edge + Sarvam gateways stay  
- [06_APPLICATION_FLOW.md](../06_APPLICATION_FLOW.md) — control/session/media boxes  
- [08](../08_PURGE_AND_SCHEMA_PHASES.md) — OD-08-1 delete three UIs; OD-08-2 delete `.agent` phases; OD-08-4 freeze `kb_*` until P2.10; OD-08-5 serial  

## goal

Freeze exact keep vs purge paths, API routes, store surfaces, and agent/lab artifacts so later P1 phases cannot invent scope.

## in_scope

- Appendices A–G in this file (path and route inventories).  
- Mapping each purge target to a **single** later phase id (P1.1–P1.12).  
- Explicit “reshape later / do not delete package” list for runtime glue.

## out_scope

- Any file deletion, route removal, or migration.  
- Designing `flow_*` tables (P2).  
- Writing P1.5–P1.12 full L3 bodies (only after P1.1–P1.4 Locked per motto).  
- Rewriting thinkpath/composer behaviour beyond naming it as P1.6 reshape.

## forbidden

- Starting P1.1 L4 without this inventory cited.  
- Soft-keeping supervisor or admin “for lab.”  
- Archiving `.agent/phases` instead of delete (violates OD-08-2).  
- DROP of `desk` / `kb_*` tables in any P1 phase.

## exit_criteria

- [x] OD-08-1…5 recorded in doc 08.  
- [x] Appendices A–G filled from repo grep/glob (date above).  
- [x] Every purge path has a owning phase id.  
- [x] Human review sign-off on this file (owner).  

## edge_cases

- N/A (documentation phase).  

## verification

- Paths listed under purge still exist on disk until their L4 phase.  
- `rg` / IDE search: no purge target missing an owning phase in Appendix B.  

## rollback

- Revert this markdown file only; no runtime effect.

---

## Appendix A — KEEP (do not purge in P1)

### A.1 Packages / trees

| Path | Why |
|---|---|
| `cmd/aiorchestrator/` | Process entry (`main.go` will lose `deskskills` register in P1.7 — file stays) |
| `internal/edge/file/` | Edge file helper |
| `internal/edge/modaudiostream/` | Dumb PCM pipe |
| `internal/edge/token/` | Edge token |
| `internal/gateway/sarvam/` | Shared Sarvam client bits |
| `internal/gateway/sarvamllm/` | LLM gateway |
| `internal/gateway/sarvamstt/` | STT gateway |
| `internal/gateway/sarvamtts/` | TTS gateway |
| `internal/gateway/ttsengine/` | TTS engine helper |
| `internal/gateway/coraltransfer/` | Transfer verb toward Coral/FS |
| `internal/gateway/fake/` | Test doubles |
| `internal/gateway/ingest/` | Ingest gateway (keep unless P1.12 proves desk-only) |
| `internal/gateway/coralcrm/` | Keep package; not V1 product focus — do not expand in P1 |
| `internal/port/` | All port contracts (`edge`, `listen`, `speak`, `think`, `skill`, `knowledge`, `registry`, `types`, `translate`) |
| `internal/runtime/session/` | Session actor / language |
| `internal/runtime/composer/` | Composer — **reshape** desk hooks in P1.6; do not delete package |
| `internal/runtime/thinkpath/` | Think path — **reshape** `Desk` controller wiring in P1.6; do not delete package |
| `internal/runtime/vad/`, `bus/`, `clock/`, `observe/`, `record/` | Live path foundations |
| `internal/control/` **except** desk-named modules listed in Appendix B | Session/media/edge/answer/callops/etc. stay; desk files removed in P1.6 |
| `internal/store/` foundations | Session/audit/transcript/credentials/KB access code **frozen**; desk store files stop use in P1.8; DROP in P2.13 |
| `docs/01`–`08`, `docs/phases/` | Architecture + planning |
| `tools/lab/Init-LabDatabase.ps1` | Lab DB init |
| `tools/lab/edge_uplink/`, `tools/lab/edge_smoke/` | Edge lab helpers |
| `tools/agent-runner/` | Runner tooling (pipelines it points at change after P1.9) |
| `migrations/` + `internal/store/migrations/` **history files** | Never rewrite applied history; new DROP migrations only in P2.13 |

### A.2 Tables — keep rows/schema for now (P1 does not DROP)

| Table | P1 rule | Later |
|---|---|---|
| `session`, transcript/audit/disposition-related | Keep | P2.2–P2.6 reshape |
| `gateway_credentials`, `system_settings`, `tenant_engines` | Keep | P2.1 |
| `caller_preference` | Keep | P2.11 |
| `session_attributes` | Keep table; desk UI/API may stop | P2.12 |
| `kb_document`, `kb_chunk` | **Frozen** (OD-08-4) — no new features | P2.10 redesign → P2.13 DROP |
| `desk`, `desk_draft`, `desk_version` | **Stop runtime use** in P1.8 | P2.7 replace → P2.13 DROP |
| `skill_invocation`, `pii_access_audit`, `erasure_request`, `consent_record` | No new desk features; inventory for P2.13 DROP list | P2.13 |

---

## Appendix B — PURGE map (path → owning phase)

### B.1 Web consoles (OD-08-1)

| Path | Phase |
|---|---|
| `web/admin/index.html` | P1.1 |
| `web/admin/app.js` | P1.1 |
| `web/user/index.html` | P1.2 |
| `web/user/app.js` | P1.2 |
| `web/supervisor/index.html` | P1.3 |
| `web/supervisor/app.js` | P1.3 |
| `web/shared/styles.css`, `web/shared/api.js` | P1.4 (only if nothing else embeds them after consoles gone) |
| `web/index.html` | P1.4 |
| `web/embed.go` (`//go:embed` of admin/supervisor/user/shared) | P1.4 |

Static mount sites (code change in P1.4 / glue): `internal/control/ui.go` — `mountStatic("/admin"|"/supervisor"|"/user"|"/shared")` and `GET /`.

### B.2 Desk brain package

| Path | Phase |
|---|---|
| `internal/desk/compile.go` | P1.5 |
| `internal/desk/model.go` | P1.5 |
| `internal/desk/engine.go` | P1.5 |
| `internal/desk/engine_langs.go` | P1.5 |
| `internal/desk/nlu.go` | P1.5 |
| `internal/desk/intent_llm.go` | P1.5 |
| `internal/desk/intent_llm_test.go` | P1.5 / P1.11 |
| `internal/desk/locale.go` | P1.5 |
| `internal/desk/locale_test.go` | P1.5 / P1.11 |
| `internal/desk/validate.go` | P1.5 |
| `internal/desk/preset_coral.go` | P1.5 |
| `internal/desk/preset_coral_xfer.go` | P1.5 |
| `internal/desk/kb_coral.go` | P1.5 |
| `internal/desk/desk_test.go` | P1.5 / P1.11 |
| `internal/desk/flow_test.go` | P1.5 / P1.11 |
| `internal/desk/xfer_flow_test.go` | P1.5 / P1.11 |

### B.3 Desk control + skills

| Path | Phase |
|---|---|
| `internal/control/desks.go` | P1.6 |
| `internal/control/desk_runtime.go` | P1.6 |
| `internal/control/desk_sandbox.go` | P1.6 |
| `internal/control/desk_api_test.go` | P1.6 / P1.11 |
| `internal/control/barge_policy.go` (desk.Doc dependency) | P1.6 reshape or delete desk-specific helpers |
| `internal/control/barge_policy_test.go` | P1.6 / P1.11 |
| Desk branches in `internal/control/runtime_adapter.go` | P1.6 reshape (file **kept**) |
| `internal/gateway/deskskills/deskskills.go` | P1.7 |
| `internal/gateway/deskskills/deskskills_test.go` | P1.7 / P1.11 |
| `deskskills.Register` in `cmd/aiorchestrator/main.go` | P1.7 |

### B.4 Desk persistence usage (stop callers; no DROP)

| Path | Phase |
|---|---|
| `internal/store/models_desk.go` | P1.8 stop exports use; file removable when no refs |
| `internal/store/store_desk.go` | P1.8 |
| `internal/store/memory_desk.go` | P1.8 |
| `Repository` desk methods in `internal/store/repository.go` | P1.8 |
| `internal/profile/validate.go` `XDesk` | P1.8 stop requiring/producing; field may linger until P2 |

Historical SQL **kept as history**: `internal/store/migrations/008_contact_desk.sql` (and root `migrations/` copies if any). DROP only via **new** migration in P2.13.

### B.5 Agent phases / pipelines (OD-08-2)

| Path | Phase |
|---|---|
| `.agent/phases/*.yaml` (entire set listed in Appendix E) | P1.9 |
| `.agent/pipelines/coral-phase.json` | P1.9 |
| `.agent/pipelines/contact-agent-cc.json` | P1.9 |
| `.agent/pipelines/product-validation.json` | P1.9 |
| `.agent/pipelines/validation-v1.json` | P1.9 |

**Keep under `.agent/`:** `config.yaml`, `secrets.example.json`, `templates/` (retarget later). Never commit `secrets.local.json`.

### B.6 Lab / publish scripts

| Path | Phase |
|---|---|
| `tools/lab/Setup-CoralDesk.ps1` | P1.10 |
| `tools/lab/publish_xfer.py` | P1.10 |
| `tools/lab/publish_tfn_v5.py` | P1.10 |
| `tools/lab/run_tfn_text_tests.py` | P1.10 |

### B.7 Desk-centric tests / harness refs

| Path | Phase |
|---|---|
| `tests/agent/scenarios/F-live-talk-india.yaml` (desk/xfer refs) | P1.11 — delete or rewrite out of desk; default **delete** if it only validates old desk |
| `tests/agent/scenarios/F-live-talk-cx.yaml` | P1.11 same rule |
| `tests/agent/features/catalog.yaml` desk entries | P1.11 strip desk features |

### B.8 Dead-code sweep candidates (confirm after P1.5–P1.8)

| Path / symbol | Phase |
|---|---|
| Remaining `XDesk` / `DeskEnd` / `OnDesk*` symbols after P1.6 | P1.12 |
| `internal/control/kb.go` | **Not** P1 delete — frozen with OD-08-4; revisit P2.10 |
| Orphan profile compile helpers only used by desk presets | P1.12 |

---

## Appendix C — HTTP routes classification

### C.1 Remove with desk API (P1.6) — from `mountDeskRoutes` in `internal/control/desks.go`

| Method + path | Notes |
|---|---|
| `GET/POST /v1/desks` | Desk registry |
| `GET /v1/desks/{id}` | |
| `PUT /v1/desks/{id}/draft` | |
| `GET /v1/desks/{id}/checklist` | |
| `POST /v1/desks/{id}/publish` | |
| `GET /v1/desks/{id}/versions` | |
| `POST /v1/desks/{id}/simulate` | |
| `POST /v1/desk-presets/{name}` | Install Coral / xfer presets |
| `GET /v1/desk-catalog` | |
| `GET /v1/desk-skills/ledger` | |
| `POST /v1/desk-skills/failures` | |
| `POST /v1/desk-skills/agents` | |
| `POST /v1/desk-skills/reset` | |
| `GET /v1/sessions/{id}/desk-state` | Desk-only |
| `GET /v1/sessions/{id}/handoff` | Desk handoff payload |
| `GET /v1/sessions/{id}/skills` | Desk skill ledger view |

### C.2 Split carefully in P1.6 (do not silent-delete without note)

| Method + path | Rule |
|---|---|
| `GET /v1/sessions/{id}/attributes` | Table stays (P2.12); **desk configurator** use goes away. Keep handler only if non-desk callers need it; else remove in P1.6 and restore under flow runtime later. |
| `PATCH /v1/sessions/{id}/disposition` | Disposition is V1 ([01](../01_VISION_AND_SCOPE.md)); prefer **keep** handler if independent of desk doc. |
| `GET/PUT /v1/tenant/properties` | Desk POC seam — **remove in P1.6** unless a non-desk caller is found in P1.0 follow-up grep during L4. |
| `GET/POST /v1/compliance/erasure*` | Desk POC compliance — **remove in P1.6**; re-plan under future compliance doc if needed. |
| `GET /v1/compliance/pii-access` | Same as erasure — **remove in P1.6**. |

### C.3 Static UI mounts (P1.4)

| Path | Phase |
|---|---|
| `GET /admin`, `GET /admin/` | P1.4 (after P1.1 files gone) |
| `GET /user`, `GET /user/` | P1.4 |
| `GET /supervisor`, `GET /supervisor/` | P1.4 |
| `GET /shared/…` | P1.4 |
| `GET /` (old console chooser `web/index.html`) | P1.4 — replace with minimal health/placeholder **only if** needed; no three-console nav |

### C.4 Keep (not desk product surface)

Examples (non-exhaustive; do not remove in P1 unless orphaned): session create/answer/stop, media, edge, transfer/hangup callops, profiles list used by ops, platform status, gateways, audit/transcript reads — owned by `internal/control/*.go` outside desks.go.

---

## Appendix D — Runtime glue to **reshape** (not delete packages)

| Location | What happens |
|---|---|
| `internal/runtime/thinkpath/path.go` — `Desk Controller`, `DeskEnd`, `DeskTransfer`, `DeskStepID` | P1.6: remove desk ownership branch; leave structure ready for future graph controller under a **new** name in a later programme phase (not P1). |
| `internal/runtime/composer/composer.go` — `OnDeskHangupArm`, `OnDeskEnd`, `OnDeskTransfer`, `Path.Desk` welcome | P1.6: remove desk callbacks; keep Speak/Listen/transfer/hangup arm patterns that match [04](../04_LIVE_TURN_MACHINE.md) for the future graph. |
| `internal/control/runtime_adapter.go` — `newDeskController`, `DeskController`, hangup/transfer desk handlers | P1.6: delete desk wiring; keep session/media adapter. |
| `internal/profile` `x_desk` JSON | P1.8: stop publish/install paths; ignore at runtime. |
| `cmd/aiorchestrator/main.go` | P1.7: unregister deskskills |

**Post-P1 acceptable state:** media/edge/Sarvam up; **dialogue brain offline**; no admin/user/supervisor; no `/v1/desks*`.

---

## Appendix E — `.agent/phases` delete set (P1.9)

All of the following (complete listing as of inventory date):

- `F-analytics-events.yaml`, `F-audit-append.yaml`, `F-control-health.yaml`, `F-control-hot-swap.yaml`, `F-control-inject.yaml`, `F-control-kb-upload.yaml`, `F-control-playback-job.yaml`, `F-control-profile-422.yaml`, `F-control-profile-crud.yaml`, `F-control-session-lifecycle.yaml`, `F-control-sse-events.yaml`, `F-edge-file.yaml`, `F-edge-modaudiostream.yaml`, `F-edge-token.yaml`, `F-fake-trio.yaml`, `F-gw-coral-crm.yaml`, `F-gw-coral-transfer.yaml`, `F-gw-ingest.yaml`, `F-job-captions.yaml`, `F-job-contact-agent.yaml`, `F-job-copilot.yaml`, `F-job-playback-mom.yaml`, `F-lock-no-pcm-broker.yaml`, `F-lock-no-secrets-in-git.yaml`, `F-lock-no-vendor-in-composer.yaml`, `F-openapi.yaml`, `F-ports-contract.yaml`, `F-postcall-enqueue.yaml`, `F-router-registry.yaml`, `F-runtime-bus.yaml`, `F-runtime-clock.yaml`, `F-runtime-composer.yaml`, `F-runtime-session.yaml`, `F-runtime-thinkpath.yaml`, `F-runtime-vad.yaml`, `F-sarvam-failover.yaml`, `F-sarvam-llm.yaml`, `F-sarvam-stt.yaml`, `F-sarvam-tts.yaml`, `F-store-migrate.yaml`, `cc-1-tenant-engines.yaml`, `cc-2-language-policy.yaml`, `cc-3-voice-binding.yaml`, `cc-4-response-ladder.yaml`, `cc-5-transcript-disposition.yaml`, `cc-6-lab-validation.yaml`, `phase-a.yaml` … `phase-f.yaml`, `validation-v1.yaml`

Rationale: mixed old platform + old contact-desk programme; agents must not treat them as current truth (OD-08-2). New phase YAML only after Implementer skill + new L3 catalog.

---

## Appendix F — Cross-check commands (for reviewers / future L4)

```text
# Consoles
# expect: admin, user, supervisor, shared, index.html, embed.go under web/

# Desk package
# expect: internal/desk/*.go

# Desk API
# rg "mountDeskRoutes|/v1/desks" internal/control

# Skills
# expect: internal/gateway/deskskills

# Store
# rg "UpsertDesk|desk_draft|FROM desk" internal/store

# Agent
# expect: .agent/phases/*.yaml
```

---

## Appendix G — Phase ownership summary

| Phase | Owns |
|---|---|
| P1.1 | `web/admin/**` |
| P1.2 | `web/user/**` |
| P1.3 | `web/supervisor/**` |
| P1.4 | embed + index + shared mounts + `ui.go` static |
| P1.5 | `internal/desk/**` |
| P1.6 | desks.go, desk_runtime, desk_sandbox, desk routes, runtime_adapter/thinkpath/composer desk branches |
| P1.7 | `internal/gateway/deskskills/**` + main register |
| P1.8 | store desk APIs / stop desk table use |
| P1.9 | `.agent/phases/*` + obsolete pipelines |
| P1.10 | Coral desk lab publish scripts |
| P1.11 | desk-centric tests + agent scenario strip |
| P1.12 | leftover desk symbols |

---

## Handoff

- Next L3 to deep-detail: **P1.1** (then P1.2, P1.3, P1.4) — one at a time.  
- Do not open P1.5 L3 body until P1.1–P1.4 are Planning Locked.  
- L4 of this phase: owner checklist sign-off only.
