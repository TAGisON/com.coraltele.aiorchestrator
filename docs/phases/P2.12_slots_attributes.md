# L3 — P2.12 Slots / session attributes

| Field | Value |
|---|---|
| **id** | `P2.12` |
| **title** | Slots / session attributes |
| **status** | Planning Locked (keep table + key policy); **owner signed 2026-09-04** (“continue”) |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P2.12 |
| **depends_on** | `P2.7` Closed; `P2.11` Closed (`7788d1e`); `P2.0` |
| **inventory_date** | 2026-09-04 — `session_attributes` in `008_contact_desk.sql` (kept; not desk DROP target) + `store.SessionAttribute` |

## architecture_refs

- [03_BRAIN_AND_GRAPH.md](../03_BRAIN_AND_GRAPH.md) — slots  
- Existing: `session_attributes` in `internal/store/migrations/008_contact_desk.sql`  
- [P1.0](./P1.0_purge_inventory.md) — table kept through P1  
- [P2.7](./P2.7_flow_publish_model.md) — ListenSlot `slot` on nodes  

## goal

Lock minimal slot key policy for V1 graph and whether `session_attributes` stays the store.

## in_scope

### Storage decision (Locked)

**Keep** table `session_attributes` (lives in migration `008` historically; **not** dropped with desk* in P2.13):

| Column | Decision | Notes |
|---|---|---|
| `session_id` | Keep | FK → session ON DELETE CASCADE |
| `key` | Keep | Slot key |
| `value` | Keep | TEXT |
| `class` | Keep | Closed: `none` \| `confidential` |
| `updated_at` | Keep | |
| PK | Keep | `(session_id, key)` |

**Migration: none** for V1 reshape.

### V1 well-known keys (closed starter set) — Locked

| Key | class | Set by |
|---|---|---|
| `active_language` | none | ListenLanguage / Entry |
| `preferred_language` | none | Entry from caller_preference |
| `department` | none | ListenChoice when flow uses this key (or exact `slot.key` from node) |
| `ani` | confidential | Entry from caller |
| `transfer_target` | none | Tool ARM from matrix |
| `transfer_number` | confidential | Tool ARM (ops; not LLM) |

Flow-defined ListenSlot keys are allowed if they match `^[a-z][a-z0-9_]*$` and are declared on the node — not an open free-for-all from LLM.

### Policy (Locked)

- LLM may propose slot **values** only when current node is ListenSlot with validation; keys never invented.  
- Tool ARM may write `transfer_*` from matrix only.  
- PII keys use `class=confidential`; transcript/audit must mask per policy (coding phase).  

## out_scope

- Cross-session long-term memory beyond caller_preference  
- Encrypting attribute values at rest (future)  
- Moving table out of `008` file history (do not rewrite applied migration)  

## forbidden

- Persisting PCM or full prompt libraries in attributes.  
- Desk-era attribute key sprawl without node declaration.  
- DROP `session_attributes` in P2.13 desk/kb purge list.  

## exit_criteria

- [x] Table keep Locked; well-known keys + key regex Locked.  
- [x] Migration: **none**.  
- [x] No DDL authored in this phase.  

## migration_plan_note

**None required.** Shape matches `008` inventory. Confirm P2.13 DROP list **excludes** `session_attributes`.

## edge_cases

1. Session ends — rows CASCADE with session delete (FK ON DELETE CASCADE).  
2. Key updates — last write wins; no history table in V1.  

## verification

Review `008` + [03](../03_BRAIN_AND_GRAPH.md) minimal voicebot — **Done 2026-09-04**.

## rollback

Revert this markdown only (planning lock).

## handoff

Next: **P2.13** DROP obsolete tables (`desk*`, `kb_*`, … — not `session_attributes`).
