# L3 — P2.13 DROP obsolete tables

| Field | Value |
|---|---|
| **id** | `P2.13` |
| **title** | DROP obsolete tables |
| **status** | Planning Locked (DROP list + sequenced M-F..M-H); **owner signed 2026-09-04** (“continue”) |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P2.13 |
| **depends_on** | `P2.7`–`P2.12` Locked (`8d2e1f9` tip); L4 DDL DROP only after replacements applied and orch no longer reads dropped tables |
| **inventory_date** | 2026-09-04 — tables in `008`/`002`; Go: `store_d.go` still touches `kb_*`; store aux still has Skill/Erasure/Consent APIs; control no longer calls them |

## architecture_refs

- [P2.0](./P2.0_schema_principles.md) P2.0-P10 expand/contract  
- [P1.0](./P1.0_purge_inventory.md) § A.2 tables  
- [P2.7](./P2.7_flow_publish_model.md) / [P2.10](./P2.10_bindings_redesign.md) / [P2.12](./P2.12_slots_attributes.md)  
- OD-08-3 / OD-08-4  

## goal

Lock the explicit DROP list and **sequenced** migration order so nothing is dropped in bulk without replacement live.

## in_scope — DROP list (Locked)

| Table | Why | Prerequisite before DROP file |
|---|---|---|
| `desk_version` | Replaced by `flow_version` | `flow_*` live; `rg` Go for desk tables → zero |
| `desk_draft` | Replaced by `flow_draft` | same |
| `desk` | Replaced by `flow` | same (drop versions/draft before or with CASCADE-safe order: version → draft → desk) |
| `kb_chunk` | OD-08-4 redesign | `binding` live; remove `store_d` / `kb.go` readers; `rg` → zero |
| `kb_document` | OD-08-4 | after `kb_chunk` (FK child first) |
| `skill_invocation` | Desk skill ledger | Remove store Repository/Memory methods + any callers; unused-confirm |
| `pii_access_audit` | Desk compliance seam | Confirm unused; remove store APIs if any; **DROP if unused** |
| `erasure_request` | Desk compliance seam | **DROP if unused** after store API removal |
| `consent_record` | Desk outbound consent | **DROP if unused** in inbound V1 after store API removal |

### Do **not** DROP in this phase (Locked)

| Table | Reason |
|---|---|
| `session`, `audit_event`, `transcript_turn`, `session_disposition` | Reshaped, not replaced |
| `caller_preference`, `session_attributes` | Kept (P2.11 / P2.12) |
| `gateway_credentials`, `system_settings`, `tenant_engines` | Kept (P2.1) |
| `binding`, `flow`, `flow_draft`, `flow_version` | New replacements |
| `profile`, `profile_version` | Transition — separate OD later if profile retired |
| `playback_job`, `postcall_job` | Out of V1 focus; **defer DROP** unless proven dead (optional follow-up) |

## Migration sequence (Locked — one concern per file)

```text
M-A  CREATE flow, flow_draft, flow_version          (from P2.7)
M-B  CREATE binding                                  (from P2.10)
M-C  ALTER session add flow pins + recording cols    (P2.2 / P2.5)
M-D  ALTER transcript_turn expand                    (P2.3)
M-E  (readers switched — runtime / control phases)
M-F  DROP desk_version, desk_draft, desk             (P2.13 file 1)
M-G  DROP kb_chunk, kb_document                      (P2.13 file 2)
M-H  DROP skill_invocation, pii_access_audit,
       erasure_request, consent_record               (P2.13 file 3 — only after unused confirm)
```

**Forbidden:** Single migration file that CREATE flow + DROP desk + DROP kb + ALTER transcript together.  
**Forbidden:** Editing applied `008` / `002` history — only **new** DROP migrations.

### Unused-confirm checklist (M-H) — Locked

Before authoring M-H:

- [ ] `rg` `skill_invocation` / `AppendSkillInvocation` → only deleted or test stubs removed  
- [ ] `rg` `erasure_request` / `ErasureRequest` / `consent_record` / `ConsentRecord` / `pii_access` → zero production readers  
- [ ] Control HTTP compliance routes already gone (P1.6)  
- [ ] Owner ack: inbound V1 does not need consent/erasure tables  

Inventory 2026-09-04: **store still implements** Skill/Erasure/Consent APIs; **control does not call them** — M-H blocked until store surface removed in a prior cleanup L4.

## out_scope

- Authoring DROP SQL in this planning L4 (plan lock only)  
- Data migration desk→flow  
- Dropping `profile*`  
- Implementing M-A..M-E (owned by earlier phase DDL waves)  

## forbidden

- DROP while any Go code still queries the table.  
- Editing old `008_contact_desk.sql` / `002_phase_d_kb.sql` history.  
- DROP `session_attributes`.  
- Mega-migration bundling CREATE+DROP families.  

## exit_criteria

- [x] DROP list Locked.  
- [x] Sequence M-F..M-H Locked with unused-confirm checklist for compliance tables.  
- [x] Do-not-DROP list includes `session_attributes` (P2.12).  
- [x] No DDL authored in this phase.  

## migration_plan_note

**Deferred DDL:** three DROP concerns (M-F, M-G, M-H) as separate new migration files after M-A..M-E + reader cutover. Snapshot DB before M-F.

## edge_cases

1. Lab DB with precious desk JSON — export manually before L4 DROP; not automated.  
2. FK from `session_attributes` to session — unaffected.  
3. `kb_chunk` before `kb_document` — respect FK.

## verification

`rg` Go for table names → zero before each DROP file — **preflight noted 2026-09-04** (`kb_*` and compliance store APIs still present → DROP L4 not yet runnable).

## rollback

L4: restore from backup only; DROP is contract step — take DB snapshot before M-F.

## handoff

Next: **P2.14** migration + CI discipline.
