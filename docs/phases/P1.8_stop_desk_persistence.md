# L3 — P1.8 Stop desk persistence usage

| Field | Value |
|---|---|
| **id** | `P1.8` |
| **title** | Stop desk persistence usage |
| **status** | Planning drafted — **no code until L4 gate** |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P1.8 |
| **depends_on** | `P1.6` (no HTTP/runtime callers) |

## architecture_refs

- [08](../08_PURGE_AND_SCHEMA_PHASES.md) OD-08-3 — replacements are `flow_*` in P2.7, not this phase  
- [P1.0 Appendix A.2, B.4](./P1.0_purge_inventory.md)  
- [03_BRAIN_AND_GRAPH.md](../03_BRAIN_AND_GRAPH.md) — graph publish model later  

## goal

Ensure no Go runtime path reads or writes `desk` / `desk_draft` / `desk_version` (or repository desk methods), without dropping tables yet.

## in_scope

- Remove or gut:

| Path | Action |
|---|---|
| `internal/store/store_desk.go` | Delete file |
| `internal/store/memory_desk.go` | Delete file |
| `internal/store/models_desk.go` | Delete file |
| `internal/store/repository.go` | Remove desk method signatures from `Repository` interface |
| `internal/store/memory.go` | Remove desk maps/fields if any remain after memory_desk delete |
| Any compile stubs | Must not no-op-write to desk tables |

- `internal/profile/validate.go` (and related): stop treating `x_desk` as required/validated publish surface; **ignore** unknown `x_desk` on read (do not fail answer). Do not invent `x_flow` here.

- Confirm `internal/control/kb.go` still compiles without desk presets (KB upload may remain frozen — OD-08-4).

## out_scope

- `DROP TABLE desk*` (P2.13)  
- Creating `flow_*` tables (P2.7)  
- Rewriting migration `008_contact_desk.sql` history  
- Deleting `kb_document` / `kb_chunk` access  

## forbidden

- New code that `INSERT`/`SELECT` desk tables “for migration tooling” inside orch runtime.  
- Renaming desk tables in place to `flow_*` without P2.7 field lock.  

## exit_criteria

- `rg "UpsertDesk|GetDesk|SaveDeskDraft|PublishDeskVersion|desk_draft|FROM desk|INTO desk" --glob "*.go"` → zero (except comments in docs if any).  
- `go build ./...` / `go test ./...` green.  
- Postgres: tables may still exist; orch must not touch them.  

## edge_cases

1. Old rows in `desk*` — orphan data until P2.13; OK.  
2. `migrate_test.go` asserting desk tables exist — update test to still allow tables present from migration 008, but not require Go APIs.  
3. Memory store tests constructing desks — delete with APIs.  

## verification

```text
go build ./...
go test ./...
rg "UpsertDesk|SaveDeskDraft|PublishDeskVersion|models_desk|store_desk|memory_desk" --glob "*.go"
```

## rollback

Restore store desk files + repository methods from git.

## handoff

Next: **P1.9** delete obsolete `.agent` phases/pipelines.
