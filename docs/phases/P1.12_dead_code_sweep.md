# L3 — P1.12 Dead code sweep (desk leftovers)

| Field | Value |
|---|---|
| **id** | `P1.12` |
| **title** | Dead code sweep after desk purge |
| **status** | Planning drafted — **no code until L4 gate** |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P1.12 |
| **depends_on** | `P1.11` |

## architecture_refs

- [02_CURRENT_STATE.md](../02_CURRENT_STATE.md)  
- [P1.0 Appendix B.8, D](./P1.0_purge_inventory.md)  
- [03](../03_BRAIN_AND_GRAPH.md) / [04](../04_LIVE_TURN_MACHINE.md) — do not invent graph here; only remove dead desk names  

## goal

Eliminate leftover desk-named symbols and dead helpers so the tree greps clean and post-P1 state is explicit.

## in_scope

Sweep and either **rename to neutral** (only if still needed) or **delete**:

| Symbol / area | Rule |
|---|---|
| `Result.DeskEnd`, `DeskStepID`, `DeskTransfer`, `Path.Desk` in thinkpath | Rename to neutral (`End`, `Transfer`, …) **or** delete if unused after P1.6 |
| `OnDesk*` names if any stub left | Delete |
| Comments citing `CONTACT_DESK_POC` / Live Talk desk sections as current law | Retarget to docs 01–06 or delete |
| Orphan helpers only used by presets/compile | Delete |
| `profile.Document.XDesk` field | May remain as ignored JSON for one release **or** remove field if no decode break; prefer remove if nothing sets it |
| Audit event strings `desk.end` / `desk.transfer` | Stop emitting; do not add new desk.* events. Mapping to future tool.* events is a **later** runtime phase — not invented here |

**Explicit non-delete:** `internal/control/kb.go`, KB store methods (OD-08-4).

## out_scope

- Graph runtime / Inform bindings  
- P2 migrations  
- New supervisor UI  
- Expanding coral CRM  

## forbidden

- Using this phase to “sneak in” a mini desk.  
- Bulk unrelated refactors (renaming half of control package).  

## exit_criteria

- `rg -i "contact desk|deskController|x_desk|PresetCoral|/v1/desks" --glob "*.go"` → zero (allow `desk` in comments only if explaining removal — prefer zero).  
- `go build ./...` and `go test ./...` green.  
- Short **Post-P1 state** note appended to this file or `docs/02_CURRENT_STATE.md` (planning edit allowed): media/edge/Sarvam up; dialogue offline; three consoles gone; desk tables orphan until P2.  

## edge_cases

1. String “desk” inside user-facing TTS samples — none expected after preset delete.  
2. DB column comments — leave SQL history alone.  

## verification

```text
go build ./...
go test ./...
rg -i "deskController|x_desk|PresetCoral|mountDeskRoutes|deskskills|OnDeskEnd" --glob "*.go"
```

## rollback

Restore swept files from git (narrow commit preferred at L4).

## handoff — P1 series complete (planning)

When P1.0–P1.12 L3 are Planning Locked and owner-signed:

1. Do **not** start L4 until [07 §8](../07_PLANNING_STANDARDS.md) gate (remaining L2 docs + Implementer skill).  
2. Next **planning** series: P2.0 schema principles L3 (only after P1 L3 series Locked per OD-08-5).  
3. Optional interim L2 docs: 09 Evidence, 10 Coding principles — may be drafted after P1 L3 lock without opening P2 field workshops in the same wave.
