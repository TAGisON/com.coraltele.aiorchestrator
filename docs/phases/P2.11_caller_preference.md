# L3 — P2.11 Caller language preference

| Field | Value |
|---|---|
| **id** | `P2.11` |
| **title** | Caller language preference |
| **status** | Planning Locked (keep-as-is + source vocabulary); **owner signed 2026-09-04** (“continue”) |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P2.11 |
| **depends_on** | `P2.0`; `P2.10` Closed (`9381fac`); aligns with [03](../03_BRAIN_AND_GRAPH.md) Entry / ListenLanguage |
| **inventory_date** | 2026-09-04 — `009_caller_preference.sql` + `store.CallerPreference` |

## architecture_refs

- [03_BRAIN_AND_GRAPH.md](../03_BRAIN_AND_GRAPH.md) — Entry optional ANI prefs → slots; ListenLanguage  
- [02_CURRENT_STATE.md](../02_CURRENT_STATE.md) — weak greeting must not lock language  
- Existing: `caller_preference` in `internal/store/migrations/009_caller_preference.sql`

## goal

Keep/reshape ANI → preferred language storage for returning callers.

## in_scope

### Table `caller_preference` — Keep (inventory = `009`)

| Column | Decision | Notes |
|---|---|---|
| `tenant_id` | Keep | PK part |
| `ani` | Keep | Normalised digits policy at runtime (E.164 or national — coding phase) |
| `preferred_language` | Keep | Locale string in Talk allowlist or empty |
| `source` | Keep | Closed vocabulary below |
| `updated_at` | Keep | TIMESTAMPTZ |

Index `caller_preference_lang_idx` (partial) — **Keep**.

**Decision:** No rename. No extra columns for V1. Migration **none**.

### `source` closed vocabulary (Locked)

| Value | Meaning |
|---|---|
| `user_said` | Written after ListenLanguage success |
| `ani_default` | Seeded from ANI/default policy |
| `ops` | Ops/admin patch |
| `legacy` | Imported/unknown historical |

Empty `source` may exist on old rows — readers tolerate; new writers use allowlist.

**Transition (code comment inventory):** `models_session_aux.go` documents `stt_lock` \| `operator` \| `default` — map to `user_said` \| `ops` \| `ani_default` in a later runtime/control pass; do not expand the Locked allowlist to those aliases.

### Runtime rules (bind later — not this phase’s code)

1. Entry may read preference into slot / seed `active_language` only if in Talk allowlist.  
2. ListenLanguage success **writes** preference (`source=user_said`).  
3. Weak greetings must not lock language ([02](../02_CURRENT_STATE.md)) — behaviour owned by graph/turn machine, not this table.  
4. Graph may still require ListenLanguage confirmation even if preference exists (**forbidden** for schema to force skip).  

## out_scope

- Full customer profile master  
- Storing name/email here  
- ANI normalisation implementation  

## forbidden

- Using preference alone to skip ListenLanguage when graph requires confirmation.  
- PII dumps beyond ANI + language.  

## exit_criteria

- [x] Keep-as-is Locked; source vocabulary Locked.  
- [x] Migration: **none**.  
- [x] No DDL authored in this phase.  

## migration_plan_note

**None required.** Live `009` matches Keep columns.

## edge_cases

1. Preferred language not in tenant Talk allowlist → ignore; treat as empty.  
2. ANI withheld — no row.  

## verification

Review `009` SQL + `CallerPreference` — **Done 2026-09-04** (1:1 Keep).

## rollback

Revert this markdown only (planning lock).

## handoff

Next: **P2.12** slots / session attributes.
