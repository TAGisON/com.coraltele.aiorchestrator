# L3 — P1.10 Remove desk lab/publish scripts

| Field | Value |
|---|---|
| **id** | `P1.10` |
| **title** | Remove desk lab/publish scripts |
| **status** | Planning drafted — **no code until L4 gate** |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P1.10 |
| **depends_on** | `P1.5`–`P1.8` preferred (APIs gone) |

## architecture_refs

- [02_CURRENT_STATE.md](../02_CURRENT_STATE.md)  
- [P1.0 Appendix B.6](./P1.0_purge_inventory.md)  
- [08](../08_PURGE_AND_SCHEMA_PHASES.md) — no resurrect install-desk path  

## goal

Delete lab scripts that install/publish Coral desk presets or drive the old text-desk test loop.

## in_scope

| Path | Action |
|---|---|
| `tools/lab/Setup-CoralDesk.ps1` | Delete |
| `tools/lab/publish_xfer.py` | Delete |
| `tools/lab/publish_tfn_v5.py` | Delete |
| `tools/lab/run_tfn_text_tests.py` | Delete |

Search `tools/` and `docs/` for references to those script names; remove or retarget mentions (docs that describe old install → point to 08/phases).

## out_scope

| Path | Keep |
|---|---|
| `tools/lab/Init-LabDatabase.ps1` | Yes |
| `tools/lab/edge_uplink/` | Yes |
| `tools/lab/edge_smoke/` | Yes |
| `tools/agent-runner/` | Yes (aside from P1.9 pipeline notes) |
| `tools/notify/` | Yes |

## forbidden

- Replacing scripts with ones that POST `/v1/desk-presets/*`.  
- Commenting out bodies but leaving files “for later.”  

## exit_criteria

- Four scripts absent.  
- `rg "Setup-CoralDesk|publish_xfer|publish_tfn|run_tfn_text" tools/` → zero (except historical notes if any outside tools — clean those too if in-repo docs claim they are current).  

## edge_cases

1. Operators’ muscle memory — document in phase summary: use edge smoke only until new flow publish exists.  
2. Python venv leftovers — out of scope.  

## verification

```text
rg "Setup-CoralDesk|publish_xfer|publish_tfn_v5|run_tfn_text_tests" tools docs
```

## rollback

Restore the four scripts from git.
