# L3 — P1.5 Delete `internal/desk` package

| Field | Value |
|---|---|
| **id** | `P1.5` |
| **title** | Delete `internal/desk` package |
| **status** | Planning drafted — **no code until L4 gate** |
| **parent_plan** | [08](../08_PURGE_AND_SCHEMA_PHASES.md) § P1.5 |
| **depends_on** | `P1.1`–`P1.4` Planning Locked (UI/shell gone before brain delete) |

## architecture_refs

- [02_CURRENT_STATE.md](../02_CURRENT_STATE.md) — discard desk hybrid; keep media pipe  
- [03_BRAIN_AND_GRAPH.md](../03_BRAIN_AND_GRAPH.md) — conversation graph replaces desk engine  
- [08](../08_PURGE_AND_SCHEMA_PHASES.md) anti-pattern: do not adapt `Engine.Turn`  
- [P1.0 Appendix B.2](./P1.0_purge_inventory.md)

## goal

Remove the entire old Contact Desk dialogue brain so later agents cannot repair or wrap it.

## in_scope

Delete the whole package tree:

| File |
|---|
| `internal/desk/compile.go` |
| `internal/desk/model.go` |
| `internal/desk/engine.go` |
| `internal/desk/engine_langs.go` |
| `internal/desk/nlu.go` |
| `internal/desk/intent_llm.go` |
| `internal/desk/intent_llm_test.go` |
| `internal/desk/locale.go` |
| `internal/desk/locale_test.go` |
| `internal/desk/validate.go` |
| `internal/desk/preset_coral.go` |
| `internal/desk/preset_coral_xfer.go` |
| `internal/desk/kb_coral.go` |
| `internal/desk/desk_test.go` |
| `internal/desk/flow_test.go` |
| `internal/desk/xfer_flow_test.go` |

Remove empty `internal/desk/` directory.

## out_scope

- Deleting `internal/control/desks.go` / `desk_runtime.go` (P1.6) — will not compile after this phase until P1.6 lands  
- Deleting `deskskills` (P1.7)  
- Store desk tables / DROP (P1.8 stop-use; P2.13 DROP)  
- Writing the new graph runtime  
- Copying presets into a “temporary” package under another name  

## forbidden

- Keeping `preset_coral_xfer` / `PresetCoralTFN` anywhere under a new path.  
- “Adapting” `Engine.Turn` into thinkpath.  
- Moving `internal/desk` to `internal/desk_legacy` or `archive/`.  
- Partial delete (leaving `model.go` “for types”).  

## exit_criteria

- `internal/desk/` absent.  
- No Go file imports `…/internal/desk` **except** that dependents may still reference it until P1.6 — **acceptable interim**: `go build ./...` **may fail** after P1.5 alone.  
- Declaring P1.5 Done requires either: (a) P1.6 also Done in the same verified wave with **separate** checklists, or (b) explicit “red build expected” note in the phase summary until P1.6. Prefer (a) for mergeability.  

## edge_cases

1. `desks.go` / `desk_runtime.go` / `runtime_adapter.go` / `barge_policy.go` import desk — broken until P1.6.  
2. Tests under `internal/desk` disappear with the package — do not re-home them.  
3. Lab profiles already published with `x_desk` remain in DB; runtime must ignore them after P1.6 (not this phase).  
4. Knowledge text in `kb_coral.go` is **not** migrated; OD-08-4 redesign later.  

## verification

```text
# internal/desk must not exist
rg "internal/desk" --glob "*.go"   # expect only history/docs or zero after P1.6
```

## rollback

Restore `internal/desk/**` from git history of pre-purge commit.

## handoff

Next: **P1.6** (must clear all remaining `internal/desk` imports).
