# Fixtures for agent validation

- `profiles/` — Control profile documents (no secrets)
- `audio/` — short WAV (8/16 kHz); add in fixture-builder wave
- `transcripts/` — optional text turns
- `kb/` / `rules/` — when product schemas require them

Never put API keys here.
